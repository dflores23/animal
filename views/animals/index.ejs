<!DOCTYPE html>
<html lang="en">
<%- include("../partials/head.ejs") %>
<body>
    <%- include("../partials/header.ejs") %>

    <main>
        <div>
            <a href="/animals/new"><button>Create new animal</button></a>
            <% for (animal of animals) { %>
                <article>
                    <a href="/animals/<%= animal._id %>">
                        <h2>
                            <%= animal.species %> <%= animal.location %> <%= animal.lifeExpectancy %>
                        </h2>
                </article>
                
            <% } %>
        </div>
    </main>
</body>
</html>